---
# vars file for artu
vsphere_definition:
  ent_dev:
    vsp_datastore: "ds-dev-nfs-templates"
    vsp_cluster: "dev-cluster"
    vsp_datacenter: "DEV"
    vsp_folder: "vm/unix"
    vsp_hostname: "pvcservercit.ad.brown.edu"